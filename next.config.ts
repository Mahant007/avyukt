/**
 * @type {import('next').NextConfig}
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactStrictMode: true,
  // output: 'export',
  // distDir: 'dist', // Optional: specify a custom output directory
  // trailingSlash: true, // Recommended for cPanel routing
  // assetPrefix: './',
};

export default nextConfig;
